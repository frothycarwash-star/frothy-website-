IMPORTANT — these 5 images are required before the site looks complete.

The code references them by these exact filenames. Drop your real photos into
this folder (public/images/) using these names, then redeploy:

  hero-car.jpg          - Hero section background/feature (Home page)
  lounge-interior.jpg   - "Not just a carwash" section (Home page, WhyFrothy)
  ceramic-coating.jpg   - Ceramic coating CTA section (Home page, Ceramic page)
  hand-wash.jpg         - About page, right column
  team.jpg              - About page, left column

Without these files, those spots will show as broken/empty image boxes on
the live site. Recommended: landscape orientation, optimized/compressed
(under ~300KB each) so they don't slow down page load.

This text file is just a placeholder/reminder — delete it once your images
are in place.
